<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
    <link rel="stylesheet" href="../styles/authNavbar.css">
    <link rel="icon" href="public/favicon.ico" type="image/x-icon">
    <meta name="viewport"
        content="width=device-width, height=device-height,  initial-scale=1.0, user-scalable=no, user-scalable=0" />
    <script type="application/javascript" src="http://teambox.local:3000/_jquery/jquery-1.12.3.js"></script>
    <!-- It must be a direkt http link to the server or it will not work, the documentation of socket.io is not quite right-->
    <script src="http://teambox.local:3000/socket.io/socket.io.js"></script>
    <meta charset="utf-8">
    <title>TeamBox</title>
</head>

<body>
    <div class="row-container">
        <div class="first-row">
            <nav>
                <input type="checkbox" id="check">
                <label for="check" class="checkbtn">
                    <span class="hamburger"></span>
                </label>
                <label class="logo"><a href="/" class="logo">TeamBox</a></label>
                <!---<h1><a href="/" class="logo">TeamBox</a></h1>--->
                <ul>
                    <% if(user && group) { %>
                        <% if(group.isActive) { %>
                            <li class="grouplogoutNavItem"><a href="/grouplogout">Group Log out</a></li>
                            <% } %>
                                <li><a href="/logout">Log out</a></li>
                                <% } else if(user){ %>
                                    <li><a href="/logout">Log out</a></li>
                                    <li class="groupSelectNavItem"><a href="/groupSelect">Select Group</a></li>
                                    <li class="groupCreateNavItem"><a href="/groupCreate" class="btn">Create Group</a>
                                    </li>
                                    <% } else { %>
                                        <li class="loginNavItem"><a href="/login">Log in</a></li>
                                        <li class="signupNavItem"><a href="/signup" class="btn">Sign up</a></li>
                                        <% } %>
                </ul>
            </nav>