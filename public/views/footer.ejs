</body>

</html>

<script>
    if (socket) {
        socket.on('shutdownPi', function (message) {
            showPopUp('simplePopup', message);
        });

        socket.on('clearAllData', function (message) {
            showPopUp('simplePopup', message);
            //Redirect to a clear cookies page.
        })
    }

</script>